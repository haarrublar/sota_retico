## First Steps


Considering using the `install-dependencies-IntelConda.py` file within a Conda environment. Run that file and later build the libraries (`pip install .`, also you can try with `pip install -e .`).

In case something happen, just go back "pull" from the safest commit. Commit flag "safe" defines that the changes within that module works fine.
